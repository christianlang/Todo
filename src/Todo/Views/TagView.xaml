<UserControl x:Class="Todo.Views.TagView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="20" d:DesignWidth="100">
    <Border CornerRadius="7" BorderBrush="#9ACAE4" BorderThickness="1" Padding="5,0">
        <Border.Background>
            <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                <GradientStop Color="#FFC4E6FA"/>
                <GradientStop Color="#FFB3D8ED" Offset="1"/>
            </LinearGradientBrush>
        </Border.Background>
        <StackPanel Orientation="Horizontal">
            <TextBlock Name="Text" />
            <Button Name="RemoveTag">
                <Button.Template>
                    <ControlTemplate>
                        <TextBlock FontSize="10" Padding="5,0,0,0">x</TextBlock>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </StackPanel>
    </Border>
</UserControl>
